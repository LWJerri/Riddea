{
    "name": "@riddea/typeorm",
    "version": "1.0.0",
    "private": true,
    "files": [
        "dist"
    ],
    "main": "dist/index",
    "types": "dist/index",
    "scripts": {
        "prebuild": "yarn clean",
        "build": "tsc -b",
        "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo"
    },
    "devDependencies": {
        "typescript": "^4.1.3"
    },
    "dependencies": {
        "typeorm": "^0.2.32"
    }
}
